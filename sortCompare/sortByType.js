function sortByType(sortArray) {
	let regCn = new RegExp('^[\\u4E00-\\u9FFF]+', 'g');
	let regEn = new RegExp('^[a-zA-Z]+', 'g');
	let regNum = new RegExp('^[0-9]+', 'g');

	let {CnArry,EnArry,NumArry}=[]
	console.log(CnArry)
	console.log(EnArry)
	console.log(NumArry)
}
